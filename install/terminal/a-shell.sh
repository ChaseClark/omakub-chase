# # Configure the bash shell using Omakub defaults
# [ -f "~/.bashrc" ] && mv ~/.bashrc ~/.bashrc.bak
# cp ~/.local/share/omakub-chase/configs/bashrc ~/.bashrc

# Load the PATH for use later in the installers
source ~/.local/share/omakub-chase/defaults/bash/shell

# [ -f "~/.inputrc" ] && mv ~/.inputrc ~/.inputrc.bak
# # Configure the inputrc using omakub-chase defaults
# cp ~/.local/share/omakub-chase/configs/inputrc ~/.inputrc

# copy bash_aliases
cp ~/.local/share/omakub-chase/dotfiles/.bash_aliases ~/